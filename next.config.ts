import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Ensure API routes are handled correctly
  experimental: {
    // This ensures API routes are properly handled
  },
};

export default nextConfig;
